This example is a prototype still.

This example only works when using QApplication, not with QGuiApplication.

To build and run it:

```
cmake -B build
cmake --build build
./build/qml5-portaldialog
```
